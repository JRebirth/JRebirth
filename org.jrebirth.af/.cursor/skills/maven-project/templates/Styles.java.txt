package ${basePackage}.resources;

import org.jrebirth.af.core.resource.style.StyleSheetEnum;

public enum ${stylesClass} implements StyleSheetEnum {

    DEFAULT {{
        ss("${styleSheetBaseName}");
    }};
}
